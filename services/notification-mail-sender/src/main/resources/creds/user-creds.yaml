kafka:
  bootstrap:
    servers: localhost:9093
  user:
    # креды нужно уточнять для remote kafka
    username: kafka
    password: kafka

# бд разворачивается локально в докере
postgresql:
  url: jdbc:postgresql://localhost:35432/mail_sender_pg?currentSchema=nf
  user:
    username: postgres
    password: postgres