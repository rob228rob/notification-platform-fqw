outbox:
  relay:
    topics:
      mail-delivery-statuses: notification.mail.delivery-statuses
      sms-delivery-statuses: notification.sms.delivery-statuses

spring:
  application:
    name: history-writer
  config:
    import: classpath:creds/user-creds.yaml
  flyway:
    locations: classpath:db/migration
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${postgresql.url}
    password: ${postgresql.user.password}
    username: ${postgresql.user.username}
  kafka:
    bootstrap-servers: ${kafka.bootstrap.servers:localhost:9093}
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP:history-writer-group}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

grpc:
  server:
    port: ${GRPC_SERVER_PORT:9091}
    auth:
      enabled: false

server:
  port: ${SERVER_PORT:8912}

logging:
  level:
    root: INFO
    ru.batoyan.vkr.notification.history.writer: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
