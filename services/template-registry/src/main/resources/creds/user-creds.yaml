kafka:
  bootstrap:
    servers: localhost:9093
  user:
    # креды нужно уточнять для remote kafka
    username: kafka
    password: kafka

# бд разворачивается локально в докере
mongodb:
  uri: mongodb://localhost:27017/template_registry
