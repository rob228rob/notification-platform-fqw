kafka:
  bootstrap:
    servers: localhost:9093
  user:
    # креды нужно уточнять для remote kafka
    username: kafka
    password: kafka

# бд разворачивается локально в докере
postgresql:
  url: jdbc:postgresql://localhost:45432/templates_registry?currentSchema=nf
  user:
    username: postgres
    password: postgres